"""RAG 引擎：文档索引 + 混合检索 + 重排。

第 2 周能力层核心。设计目标：
- 语义分块（重叠窗口）
- Embedding：未配置 Key 时使用确定性演示向量（同文本恒稳定），保证无 Key 可跑通全链路
- 检索：向量余弦 + BM25 关键词，RRF 融合
- 重排：轻量交叉分数提升（供第 3 周换 bge-reranker 的演进预留）

当前向量库为进程内实现（pydantic 数据 + 字典），第 4 周迁移 pgvector。
"""