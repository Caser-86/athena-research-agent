"""RAG 引擎：文档索引 + 混合检索 + 重排。

能力层核心。设计目标：
- 语义分块（重叠窗口）
- Embedding：未配置 Key 时使用确定性演示向量（同文本恒稳定），保证无 Key 可跑通全链路
- 检索：向量余弦 + BM25 关键词，RRF 融合
- 重排：轻量交叉分数融合；可在后续按评测结果替换为交叉编码器

向量库支持进程内实现与 PostgreSQL + pgvector，通过 ATHENA_VECTOR_STORE 切换。
"""
